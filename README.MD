## ƒомашнее задание к ”року 2. —инхронизации: многопоточность, создание и завершение потоков:

ƒобавьте возможность ввести слово Exit в чате клиента, чтобы можно было завершить его работу. 
¬ коде сервера добавьте ожидание нажати€ клавиши, чтобы также прекратить его работу.

---
## –ешение:

ƒл€ решени€ задачи был реализованы следующие действи€:
1. ƒл€ выхода из чата клиента, в классе UdpClientApp  был добавлен следующий код (стр. 36-46):
```

            if (UserMessage == "Exit")
            {
                UserMessage = $"The user {Name} has left the application.!";

                Name = "";

                SendMessage();

                client.Close();
                Environment.Exit(0);
            }

```
ѕользователь клиентской части приложени€, введ€ в консоле слово Exit, завершает работу клиенсткого  приложени€.

2. ƒл€ выхода из приложени€ сервера, в классе UdpServer был добавлен следующий код (стр. 21 - 30 ):
```
            if (Console.KeyAvailable == true)
            {
                server.Close();
                var key = Console.ReadKey(true).Key;

                Console.WriteLine(key);

                Console.WriteLine("Server stoped!");
                Environment.Exit(0);
            }

```

ѕользователь серверной части приложени€, введ€ кнопку Enter на клавиатуре завершает работу серверного приложени€. –еализованный способ имеет недостатки(работа серверной части завершаетс€ непредсказуемо). 